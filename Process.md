# 多免疫检查点小分子AI筛选程序设计方案
（DiffDock 初筛 + AlphaFold3 精细模拟）

## 1. 项目概述
### 1.1 研发目标
针对 **PD-1、LAG-3、TIM-3、VISTA** 四大肿瘤免疫检查点靶点，实现两套小分子药物研发路径的自动化计算筛选：
1.  **已知库筛选**：从指定小分子化合物库中，快速筛选出能与靶点蛋白特异性结合、潜在具有抑制活性的分子
2.  **特征驱动全新设计**：提取起效/不起效药物的结构与理化特征，生成全新小分子后再完成活性评估

整体采用「**DiffDock 快速对接初筛缩小范围 → AlphaFold3 蛋白-配体复合物高精度模拟 → 多维度相互作用定量分析**」的分级策略，最终输出高优先级候选分子清单，支撑后续实验验证。

### 1.2 整体技术框架
```
路线1：已知库筛选
小分子库 → 标准化预处理 → DiffDock批量对接 → 初筛排序过滤 → AlphaFold3精细模拟 → 相互作用分析 → 终选候选

路线2：全新分子设计
活性/非活性数据集 → 特征提取 → 活性模型训练 → 新分子生成 → 活性初筛 → 接入路线1后续流程
```

---

## 2. 环境搭建与依赖准备
### 2.1 基础虚拟环境创建
使用 Conda 管理依赖，避免版本冲突，推荐 Python 3.10 版本（兼容绝大多数生物计算与深度学习工具）。
1.  创建名为 `immuno_drug_screen` 的虚拟环境
2.  激活环境后，统一安装所有依赖库

### 2.2 虚拟环境需安装的 Python 库
按功能分类安装，所有库均在激活的虚拟环境内安装：

| 类别 | 核心库 | 用途说明 |
|------|--------|----------|
| 化学信息学 | RDKit、OpenBabel（Pybel） | 小分子结构标准化、格式转换、3D构象生成、理化性质计算、结构指纹提取 |
| 蛋白结构处理 | Biopython、PDBFixer、ProDy | 蛋白结构读取、缺失残基/侧链修复、结构域提取、结构对齐 |
| 相互作用分析 | ProLIF、MDAnalysis | 定量计算蛋白-配体的氢键、疏水作用、盐桥、π堆积等非共价相互作用 |
| 深度学习框架 | PyTorch、PyTorch Geometric | 支撑 DiffDock 等深度学习模型运行，需匹配本机 CUDA 版本 |
| 数据与工程工具 | NumPy、Pandas、SciPy、tqdm、PyYAML、Loguru | 数据处理、进度管理、配置文件读取、运行日志记录 |
| 可视化工具 | py3Dmol、Matplotlib | 复合物结构可视化、结果统计图表绘制 |

### 2.3 需从 GitHub 下载的第三方工具
#### 2.3.1 DiffDock（扩散模型分子对接工具）
- **仓库地址**：https://github.com/gcorso/DiffDock
- **核心用途**：大规模小分子库的快速对接初筛，基于扩散模型生成结合构象，速度远快于传统对接方法
- **部署步骤**：
  1.  克隆仓库到项目目录
  2.  在虚拟环境中安装仓库内 `requirements.txt` 的依赖
  3.  运行权重下载脚本，获取官方预训练模型
- **注意**：需保证与虚拟环境的 PyTorch、CUDA 版本兼容

#### 2.3.2 AlphaFold 3（复合物高精度结构预测）
- **仓库地址**：https://github.com/google-deepmind/alphafold
- **核心用途**：对初筛得到的候选分子，进行蛋白-小分子复合物的端到端高精度结构预测，模拟真实结合模式
- **部署方式（推荐二选一）**：
  1.  **Docker 部署（推荐）**：拉取官方 Docker 镜像，避免 JAX、OpenMM 等依赖冲突
  2.  **本地部署**：手动安装 JAX、OpenMM、HH-suite 等全套依赖，适合定制化开发
- **配套数据**：需下载官方序列数据库与小分子参数库，完整数据库约 2TB；小批量测试可使用精简数据集

#### 2.3.3 可选辅助工具
- 可补充下载 AutoDock Vina，用于对接结果的交叉验证
- 可补充安装分子力场工具（如 AMBER），用于后续分子动力学补充验证

---

## 3. 数据准备规范
### 3.1 靶点蛋白结构数据
1.  **结构来源**
    - 优先从 RCSB PDB 数据库下载四个靶点的**高分辨率胞外域晶体结构**，优先选择带配体的共晶结构
    - 无合适晶体结构时，从 AlphaFold DB 下载官方预测结构
2.  **预处理要求**
    - 去除结晶水、多余肽链、共晶配体与其他杂原子
    - 修复缺失的侧链原子与 loop 区域
    - 在生理 pH 7.4 条件下添加氢原子
    - 裁剪保留功能结构域（如 PD-1 的 IgV 结构域），去除跨膜区与胞内区
3.  **文件规范**：统一为 PDB 格式，按靶点命名，存放于项目专用目录

### 3.2 小分子化合物库数据（路线1专用）
1.  **输入格式**：支持 SMILES 文本文件（`.smi` / `.csv`）或 SDF 分子库文件
2.  **预处理要求**
    - 去除盐离子、溶剂片段，完成结构标准化
    - 去重并剔除无效、错误结构
    - 生成合理三维构象（如 ETKDG 算法）并做基础力场优化
    - 计算分子量、logP、氢键供体/受体数等基础理化性质
3.  **输出规范**：统一输出为带 3D 构象的 SDF 文件，每个分子携带唯一 ID

### 3.3 活性/非活性分子数据集（路线2专用）
1.  **数据来源**：从 ChEMBL、BindingDB 等数据库，收集四个靶点的已知抑制剂（阳性样本）与非活性分子（阴性样本）
2.  **数据标注**：根据 IC50 / EC50 阈值，明确划分「起效」与「不起效」类别
3.  **数据划分**：按比例拆分为训练集、验证集，用于后续活性预测模型训练

---

## 4. 路线一：已知小分子库虚拟筛选详细流程
### 4.1 流程总览
数据预处理 → DiffDock 批量对接初筛 → 多维度过滤排序 → AlphaFold3 复合物精细模拟 → 结合模式定量分析 → 输出终选候选榜单

### 4.2 步骤1：数据预处理
1.  批量处理四个靶点的蛋白结构，完成去杂、修复、加氢，输出标准化 PDB 文件
2.  处理输入的小分子库，完成结构标准化、去重、3D 构象生成与优化
3.  同步执行成药性初筛（如 Lipinski 五规则），提前排除成药性极差的分子
4.  输出统一格式的输入文件，供后续对接使用

### 4.3 步骤2：DiffDock 批量对接初筛
1.  依次对四个靶点，批量提交小分子库进行对接计算
2.  每个分子生成多个对接构象，记录每个构象的置信度分数与结合姿态
3.  对每个分子提取最佳对接构象的分数，作为初筛核心指标
4.  自动汇总所有靶点的对接结果，生成统一的分数统计表

### 4.4 步骤3：初筛候选过滤与排序
1.  按对接置信度从高到低初步排序
2.  叠加成药性、结构新颖性、合成可及性等维度，进行加权综合打分
3.  **位点校验**：确认小分子结合区域位于靶点的功能界面（如 PD-1 的 PD-L1 结合面），剔除结合在无关区域的假阳性分子
4.  每个靶点筛选出 Top 50~100 个分子，进入下一阶段精细模拟

### 4.5 步骤4：AlphaFold3 复合物精细结构模拟
1.  将初筛候选分子与对应靶点蛋白一一配对，格式化 AlphaFold3 所需的输入文件（蛋白序列 + 小分子结构）
2.  批量提交任务，进行蛋白-小分子复合物的高精度结构预测
3.  提取每个复合物的结构置信度（pLDDT、配体结合置信度），剔除结构质量过低的结果
4.  输出所有可靠的复合物三维结构文件

### 4.6 步骤5：结合模式定量分析与终选排序
1.  对每个可靠复合物，计算详细的相互作用指纹：氢键、盐桥、疏水相互作用、π-π 堆积等
2.  统计小分子与靶点功能关键残基的相互作用数量，评估位点匹配度
3.  基于结构经验公式，估算结合自由能与亲和力
4.  综合「对接分数、结构置信度、相互作用强度、成药性」多维度指标，计算综合得分并排序
5.  输出最终候选分子优先级榜单，附带每个分子的结合模式说明与后续实验建议

---

## 5. 路线二：基于活性特征的全新分子设计流程
### 5.1 流程总览
活性数据集构建 → 分子特征提取 → 活性预测模型训练 → 全新分子生成 → 活性初筛 → 接入路线一的对接-精细模拟流程

### 5.2 步骤1：分子特征提取
1.  对标注好的起效/不起效分子，提取三类特征：
    - 理化描述符：分子量、logP、拓扑极性表面积、氢键数、可旋转键数等
    - 结构指纹：ECFP4 指纹、MACCS 密钥、药效团特征等
    - 图结构特征：原子属性、键属性，用于图神经网络模型
2.  完成特征标准化与筛选，去除冗余特征

### 5.3 步骤2：活性预测模型训练
1.  选择合适的模型（随机森林、GNN、分子 Transformer 等）
2.  使用训练集训练二分类/回归模型，预测分子对靶点的抑制活性
3.  在验证集上评估模型性能（AUC、准确率、相关性系数等），优化模型参数
4.  保存训练完成的高可靠活性预测模型

### 5.4 步骤3：全新分子生成与初筛
1.  基于分子生成模型（如 VAE、扩散生成模型、强化学习生成模型），批量生成全新小分子结构
2.  用训练好的活性预测模型，对所有生成分子进行活性打分
3.  过滤重复结构、违反成药性规则的结构，保留预测活性靠前的分子
4.  输出全新候选分子库，接入路线一的后续筛选流程

### 5.5 步骤4：对接与精细验证
将生成的全新分子库，代入路线一的步骤 2~5，完成 DiffDock 对接、AlphaFold3 精细模拟、终选排序。

---

## 6. 需编写的 Python 程序模块说明
无需从零实现算法，核心是编写调度与分析脚本，串联第三方工具与数据流程。以下为需要编写的核心脚本及其功能：

### 6.1 `data_preprocess.py` — 数据预处理脚本
- **核心功能**：统一完成蛋白结构预处理与小分子库标准化
- **输入**：原始靶点 PDB 文件、原始小分子库文件
- **输出**：预处理后的蛋白 PDB、标准化 3D 构象小分子 SDF、理化性质统计表
- **执行逻辑**：批量处理四个靶点的结构修复；完成小分子去盐、构象生成、成药性参数计算

### 6.2 `diffdock_batch_run.py` — DiffDock 批量调度脚本
- **核心功能**：自动化调用 DiffDock，完成多靶点、批量小分子的对接任务
- **输入**：预处理后的靶点结构、小分子库、对接参数配置
- **输出**：每个靶点的对接结果目录、对接分数汇总 CSV
- **执行逻辑**：循环遍历四个靶点提交任务；自动解析输出提取最佳构象分数；支持断点续跑

### 6.3 `primary_screen_filter.py` — 初筛排序与过滤脚本
- **核心功能**：对对接结果做多维度过滤，筛选进入精细模拟的候选
- **输入**：对接分数表、理化性质表、靶点功能位点残基列表
- **输出**：初筛 Top N 候选分子清单、排序详情表
- **执行逻辑**：多参数加权打分；结合位点校验；按靶点输出指定数量的候选

### 6.4 `af3_complex_prediction.py` — AlphaFold3 批量调用脚本
- **核心功能**：批量提交蛋白-小分子对，完成复合物结构预测
- **输入**：初筛候选分子列表、靶点蛋白序列
- **输出**：每个复合物的预测结构 PDB、置信度文件
- **执行逻辑**：自动格式化输入文件；支持 Docker/本地两种调用模式；提取结构置信度并做初步质量过滤

### 6.5 `interaction_analysis.py` — 复合物相互作用分析脚本
- **核心功能**：定量分析蛋白-配体相互作用，评估结合质量
- **输入**：AlphaFold3 输出的复合物 PDB 文件
- **输出**：相互作用指纹表、关键残基作用统计、结合亲和力估算值
- **执行逻辑**：计算各类非共价相互作用；统计功能位点残基的作用数量；生成相互作用可视化标注

### 6.6 `final_ranking.py` — 候选分子终选排序脚本
- **核心功能**：综合所有维度，输出最终候选分子优先级榜单
- **输入**：对接分数、结构置信度、相互作用分析结果、理化性质
- **输出**：最终排名报告、综合得分表
- **执行逻辑**：设定多维度权重计算综合得分；自动生成带说明的候选报告

### 6.7 `activity_model_train.py` — 活性特征与模型训练脚本（路线二专用）
- **核心功能**：提取分子特征，训练活性预测模型
- **输入**：标注好的活性/非活性分子数据集
- **输出**：训练好的活性预测模型、特征重要性报告
- **执行逻辑**：多类型特征提取；多模型训练与评估；自动保存最优模型

### 6.8 `molecule_generation.py` — 全新分子生成脚本（路线二专用）
- **核心功能**：基于活性特征生成全新小分子并完成初筛
- **输入**：训练好的活性预测模型、生成参数配置
- **输出**：高活性预测得分的全新小分子库
- **执行逻辑**：批量生成新结构；活性预测打分；自动过滤无效与违规结构

---

## 7. 项目目录结构建议
```
immuno_checkpoint_screen/
├── config/                  # 配置文件（靶点路径、筛选参数、模型参数）
├── data/
│   ├── targets/             # 靶点蛋白原始与预处理后结构
│   ├── library/             # 原始小分子库
│   └── activity_dataset/    # 活性/非活性数据集（路线二）
├── tools/
│   ├── DiffDock/            # 克隆的 DiffDock 仓库
│   └── alphafold/           # 克隆的 AlphaFold3 仓库
├── scripts/                 # 上述所有 Python 脚本
├── results/
│   ├── diffdock/            # DiffDock 对接结果
│   ├── primary_screen/      # 初筛排序结果
│   ├── alphafold3/          # AlphaFold3 复合物结果
│   └── final_report/        # 终选报告与分析
└── logs/                    # 运行日志
```

---

## 8. 注意事项
1.  **计算资源**：DiffDock 与 AlphaFold3 均推荐使用 GPU 加速；大规模库筛选建议部署在服务器集群上
2.  **效率平衡**：大规模库先用 DiffDock 快速缩小范围，小批量候选再用 AlphaFold3 精细计算，兼顾效率与精度
3.  **假阳性控制**：必须结合靶点功能位点做校验，排除非特异性结合的分子；计算结果仅为预测，最终需生化、细胞实验验证
4.  **版本兼容**：安装时严格匹配 PyTorch、CUDA、DiffDock、AlphaFold3 的版本要求，避免运行报错